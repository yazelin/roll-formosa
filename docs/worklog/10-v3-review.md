# Phase 11 作業ログ: v3レビュー/検証（ワークフロー `katamari-v3-review`）

**手法**: 3次元レビュー（継ぎ目/ペーシング/モバイルUX）→ 敵対的検証 → 修正（シード4件込み）→ 実機最終検証。初回実行はフィクサー到達時点でセッショントークン上限に到達して停止 → ユーザーのトークン回復後、`resumeFromRunId` でレビュー/検証フェーズのキャッシュを温存したまま再開（再実行されたのはフィクサーとバリデータのみ）。

**実行実績**: 初回27エージェント(約212万トークン) + 再開27エージェント枠中2实行(約144万トークン) / 合計約356万トークン

**注**: 再開時、敵対的検証はレビュー時点のスナップショットではなくHEADに対して検証したため、「初回実行中に並行して修正済みの指摘」を陳腐化として棄却するものが多数出た（dash ring の @property inherits、オーバーレイのflexクリッピング、ジッター矩形クランプ等は初回フィクサー実行前にすでに統合時修正が入っていた/再開前にコミット済みだった）。

**結果**: 確定 6件 / 棄却 16件 / 修正 10件 / 最終検証 **GO FOR DEPLOY**

## 確定指摘

| 次元 | 深刻度 | 指摘 |
|---|---|---|
| seams | low | devTeleport has no finale guard: teleporting post-contact re-arms the Skytree collider against MERGE writes and permanently stalls streaming |
| seams | low | Curated overflow fallbacks execute cleanup/LANDMARK during the ABSORB dispatch, violating the frozen deferral and ordering contracts |
| seams | medium | No curated preload: title screen shows the shop with zero curated dressing, and every run starts with a 7-frame staggered materialization |
| mobile | low | Tap targets below 44px: #mute-button is 36x36 and #donack-toggle is ~38px tall |
| mobile | low | Sub-10px text at 360px (7-9px labels) and missing -webkit-text-size-adjust lets iOS landscape inflate the locked layout |
| mobile | low | iOS AudioContext 'interrupted' state is not resumed by the visibilitychange handler (only 'suspended' is checked) |

## 修正内容

- **PACING RUNAWAY (seeded, critical): empirical validation + rank retune**: Drove two full in-browser runs (agent-browser + vite dev, synthetic-key bot reading window.__v3dbg, sim time from the HUD timer) along the real path shop->street->Akiba->dome->station->diet->tower->Skytree. The shipped fix set (growthKForObjR taper + DENSITY_K_BY_BAND, already wired in absorb.js with landmark/collectible exemption) holds: the old 4m->117m/3s Akiba cascade is gone (4m@135.6s -> 12m@157.4s -> 37m@203.3s on the optimal run); no non-authored second exceeded x1.41 radius growth outside the bounded shop interior. Retuned RANK_S/A/B/C and TIME_BONUS_FULL/ZERO in src/config/tuning.js to the measured model (S = ~1.2x the measured 243s optimal).
- **MOBILE OVERLAP SWEEP (seeded): verified clean at all 4+ viewports**: Forced worst-case toast + collect-popup + Donack bubble simultaneously and measured getBoundingClientRect overlaps at 360x800, 390x844, 430x932 portrait and 844x390, 932x430 landscape: ZERO overlaps (the known 390px toast/popup overlap is resolved by the integration row layout — toast bottom 115px < popup top 118px < donack top 206px; verified empirically with screenshots /tmp/v3fix/02-overlap-390.png, 03-overlap-*.png). Donack bubble never reaches the joystick band, dash button, or score pill at 390px.
- **RESCALE PIXEL-IDENTITY EVIDENCE (seeded): KeyR during play + finale approach, crossfade handoff**: Settled-ball before/after KeyR pairs vs same-cadence ambient baselines at ws 0.2->1.0 (street play) and ws 625->3125 (finale approach with Skytree mesh + Tokyo Tower in frame): ball/world/Skytree/backdrop regions show zero diff even at x6 amplification — only sub-pixel AA shimmer on grid lines and HUD timer digits (big-delta pixels 0.195% approach / 2.2% play incl. a Donack bubble dismissing mid-pair). Silhouette->mesh handoff captured across the KeyR ws 0.2->1.0 latch (dist 1010 sim < 3200 handoff): tower shape continuous, no double image, no gap (/tmp/v3fix/14-*.png, 22-approach-*.png, 26-handoff-*.png, 27-crop-*.png).
- **DONACK LIVE CADENCE (seeded): measured spam, retuned gaps, re-verified**: Logged every bubble (MutationObserver, sim timer + wall clock) over two full driven runs. Run 1 (gaps 8/4): 35 bubbles, avg 12s gap = spam. Fix: DONACK_GAP_P01_S 8->16, DONACK_GAP_P2_S 4->10 in tuning.js. Run 2 re-measure: mid-game avg gap 32.0s (>=20s required); sub-20s gaps only in the P3 landmark-trivia endgame chain (gap-exempt by design). Verified: landmark lines fire on matching landmark absorbs (toast/bubble timestamps align), first-absorb-per-category lines fire, only the scripted goal_contact/ascension/result lines show during the cinematic, and the 390px bubble rect never covers joystick/dash/score.
- **devTeleport finale guard (confirmed finding, low)**: Early-return with console.warn when finale.inputLocked in src/main.js devTeleport. Verified live: devTeleport('shop') during ASCENSION returned false with the warning and the cinematic completed normally.
- **Curated overflow fallbacks violate deferral/ordering contracts (confirmed finding, low)**: src/world/curated.js _onAbsorb: defer-queue overflow now drains the OLDEST deferred entry (its dispatch already completed) via copyWithin and appends the in-dispatch placement — the deferral contract holds for the live dispatch; landmark-queue overflow is now a DEV assert (11 landmarks can never exceed the 16 cap) instead of an in-dispatch EVT.LANDMARK emit.
- **No curated preload: empty title-screen shelves + 7-frame start materialization (confirmed finding, medium)**: Added CuratedSpawner.preload() (one full-placement pass via the existing _forceScanPending machinery) and called it after all three spawner.preloadStartArea sites in main.js (boot, resetWorld, devTeleport). Verified in-browser: curated.aliveCount = 241 at the title screen before GAME_START.
- **Tap targets below 44px (confirmed finding, low)**: index.html: #mute-button::after inset:-6px hit-area extender (36px visual -> 48px effective; verified elementFromPoint 4px outside the visual edge still hits the button) and #donack-toggle min-height:44px + padding 11px 22px (computed style verified).
- **Sub-10px text at 360px + missing text-size-adjust (confirmed finding, low)**: index.html: html/body -webkit-text-size-adjust/text-size-adjust:100%; .collect-cell 8->9px, .cell-new 7->8px (padding 1px 4px), #tier-label 9->10px; at max-width:360 the cell name spans are display:none (thumbnail carries the 48px cell). Verified computed text-size-adjust = 100% in-browser.
- **iOS AudioContext 'interrupted' not resumed (confirmed finding, low)**: bgm.js visibilitychange + _onGesture and sfx.js _onGesture guards changed from state==='suspended' to state!=='running' so the non-standard iOS 'interrupted' state resumes; the nextNoteTime re-anchor stays unconditional on the visible branch (no backlogged scheduler burst).

**スキップ（理由付き）**:

- **BatchedMesh multi_draw fallback measurement (KNOWN-list item)**: Not in the SEEDED set or confirmed findings, so outside the fix mandate; measuring the non-multi_draw path needs a WEBGL_multi_draw-disabled environment (extension blocklist or older GPU stack) that this headless Chrome-for-Testing setup does not expose. Flagged for the Phase-3 profile pass.
- **Shop-interior per-second growth spikes (x1.7-2.8/s at R 0.05-0.25m)**: Deliberately not retuned: the growthKForObjR <=0.1m K=10 exemption IS the binding authored shop budget (~60s, tuning.js comment), the spikes only occur under frame-perfect greedy chaining of adjacent shelf-ladder items at cm scale, and shop supply is volumetrically bounded (sum R^3 caps shop-exit radius at ~0.54m) — a bounded authored ladder step, not the unbounded corridor cascade the 1.5x invariant targets. Tapering it would break the authored 60s shop budget.

## 実測ペーシング

Driven full runs (agent-browser bot, sim time from HUD timer, 1Hz radius sampling). RUN A optimal-proxy (greedy nearest-absorbable, seed 12345): goal contact 4:02.9 (242.9s) — r0.42m@67s (shop), 2.2m@125s (street), 4m@135.6s (release), 12m@157.4s, 37m@203.3s (Akiba), 85m@221.5s, 136m@230.1s, 262m/420m@241.8s (ladder). RUN B typical-proxy (2.5s retarget handicap, seed 777): goal contact 7:17.5 (437.5s) — 0.42m@197s, 2.2m@276s, 4m@289s, 12m@328s, 37m@399s, 420m@434s. Targets met: optimal 4:03 within 3.5-4.5min; typical 7:17 within 5-8min full first-clear. Per-held-second radius ratios: every >1.5x second outside the shop contains an authored landmark K=10 jump (x1.55 design, e.g. station 135->210, diet+tower chain x2.24/s at R=247 — exempt); max NON-authored filler ratio x1.41/s (Akiba) / x1.44 (city). Shop interior shows x1.7-2.8/s transients at R=0.05-0.25m under frame-perfect chaining — left as designed: the <=0.1m K=10 exemption is the binding authored ~60s shop budget and shop supply is volumetrically bounded (sum R^3 caps exit at ~0.54m), so no runaway is possible. Static model (scripts/pacing-model.mjs) agrees: corridor hold-W inert, max non-special x1.47/s. Zero console errors across all runs.

**新ランク閾値**: RANK_S_S=290, RANK_A_S=400, RANK_B_S=540, RANK_C_S=720 (was 240/330/450/600); TIME_BONUS_FULL_S=290, TIME_BONUS_ZERO_S=720 (was 240/600). Anchored to the measured frame-perfect optimal of 243s: S = ~1.2x optimal (290s), A = brisk clear ~1.65x, B/C bracket the measured 437s typical-proxy first clear (lands B with a visible A/S ladder).

## 敵対的検証で棄却された指摘

### ✗ devTeleport big-to-small leaves curated release latch stale: shop shell becomes a giant pushback sphere inside restored walls（seams）

The finding's mechanism was technically accurate against committed HEAD (d07d91c) — forceScan() there did not re-arm _released/_dropT while terrain.reset() did, and the release-gated shop shell (cityMap.js:589-602, radiusReal 4.0, landmarkId -1, releaseGated true) would have activated as a ~100-sim-unit non-special object around the ball after a big→small devTeleport, with _restY keeping shelf items floor-collapsed. HOWEVER, the defect no longer exists: the current working tree contains the exact suggested fix, applied today (uncommitted diff, curated.js mtime 11:43): `this._released = false; this._dropT = 0;` added in CuratedSpawner.forceScan() (now curated.js:365-371) with a comment matching the suggested-fix wording verbatim. The finding's stale line numbers (forceScan 338-353, latch 402-405) confirm it was generated against the pre-fix HEAD. Fix correctness verified: update() step 2 (curated.js:422-425) re-latches from live ballRadiusSim*worldScale BEFORE the deferred budget-free full pass (step 3), so small→big teleports still release on the first post-teleport tick and big→small teleports restore the pre-release shop state. No remaining divergence path between the terrain and curated latches. The finding is refuted as already-fixed/not-present in the current code; nothing actionable remains. (If assessed pre-fix, severity 'high' was also somewhat inflated: the path is DEV-only — window.devTeleport is gated by import.meta.env.DEV, and the prod ?at= boot path starts with both latches false — though it would have disrupted the binding Phase-3 retune workflow.)

### ✗ devTeleport replays one TIER_UP per tier step in a single frame — toast/jingle/donack/bgm burst（seams）

The emission mechanism is real: devTeleport (main.js:539) zeroes tierIndex and maybeTierUp's while-loop (scaleManager.js:170-173) calls _emitTierUp per step, so ?at=goal&r=400 fires 6 TIER_UP events in one frame (7-tier table, tiers.js). However, every claimed user-visible consequence fails under inspection. (1) On the ?at= boot path — the one pacing-evaluation sessions actually use — devTeleport runs at module load before any user gesture: Sfx's AudioContext is null (created on first input; _silent() at sfx.js:337 gates the arpeggio), Bgm._onTierUp (bgm.js:300-305) early-returns on ctx===null/!playing and merely sets _tier=6 (last-write-wins, which is the CORRECT post-teleport state), Donack is in _phase='title' while all tier lines are phase:PLAY so _trigger drops all 6 at donack.js:458, and the HUD has no toast queue — _onTierUp (hud.js:368) overwrites one banner element, which GAME_START's hud.reset() then hides anyway. env/backdrop are explicitly snapped, and the author's comment at main.js:567-571 shows the multi-emit ladder was known and deliberately handled for the only crossfading consumers. The burst is fully inert on this path, so 'it pollutes exactly the sessions used for pacing evaluation' is false. (2) On the mid-run console path (window.devTeleport), donack's queue-of-1 discards equal-priority candidates (donack.js:511-513, all tier lines are P2), so at most one line surfaces — the 'enqueues per-tier lines' claim is wrong; hud shows the final tier name once; bgm's 6 redundant gain ramps converge to the same state. The only genuine artifact is 6 same-frame sfx arpeggios stacking into one louder jingle — dev-console-only, cosmetic, sub-second. The suggested snapTier fix is harmless hygiene (and mild future-proofing for new TIER_UP consumers), but no observable defect exists on the spec'd dev-start path, so the finding as stated does not hold.

### ✗ BatchedExtraPool.alloc leaves the previous tenant's tint in _colorHex; getColorHex can report a stale color if setColor is ever skipped（seams）

The low-level mechanics are accurately described (alloc/free at /Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/fableDemoGame/src/render/extraPools.js:122-159 never reset _colorHex; three r177 BatchedMesh recycles instance ids; getColorHex:190-193 guards only _alive). However, the stale-read scenario is unreachable: curated.js:669 is the sole allocator of EXTRA pool slots, and its alloc path (curated.js:670-677) calls pool.setColor(slot, hex) UNCONDITIONALLY whenever slot >= 0 — the colorHex<0 case the finding cites as the skip path actually resolves a fallback palette color (pal[pi % pal.length]) and still calls setColor. Color resolution cannot fail, alloc and setColor are in the same synchronous call (no interleaved read possible in single-threaded JS), and the only getColorHex consumer (main.js:293 ABSORB attach handler) reads a live slot that therefore always carries its current tenant's tint. The finding's own text concedes "today always" / "purely defensive today" — it is a hardening suggestion, not a defect, and its one concrete mechanism (setColor skipped on the colorHex<0 fallback) misreads the code. The suggested one-line reset is harmless polish but fixes nothing reachable.

### ✗ Growth cascade is structural: GROWTH_K=10 x ABSORB_RATIO 0.65 makes every near-threshold absorb a x1.554 radius jump, and absorb rate scales with R^2 — density retune alone cannot fix it（pacing）

The finding's mathematics is correct and verified independently (near-threshold absorb at ABSORB_RATIO 0.65 with K=10 gives x1.5531 radius jump; capture rate ~R^2 via 8.5R speed and ~2.9R sweep width; the pre-fix cascade was super-exponential and the 4m->117m/3s integration symptom was real). However, the finding is STALE against the current working tree: the exact remediation it proposes is already implemented and validated. src/config/tuning.js:337-349 defines growthKForObjR (K=10 for objects <=0.1m real, power-law taper to floor 2 by ~3.6m), explicitly commented as the 'CRITICAL pacing fix — the 4m->117m/3s cascade'. src/physics/absorb.js:239-246 applies it per absorb with the proposed curated exemption (collectibleId >= 0 || landmarkId >= 0 ? GROWTH_K : growthKForObjR(r * ws)), preserving the authored landmark ladder including the binding Tokyo Tower 262->406 ramp; curated.landmarkIdFor/collectibleIdFor exist (src/world/curated.js:299-312). Per-band density DENSITY_K_BY_BAND = [0.45,0.45,0.3,0.3,0.2,0.2,0.15] is live in src/world/spawner.js:801/804. Running scripts/pacing-model.mjs (which now models the SHIPPED law; the finding's pre-fix 'section E/G' framing no longer exists in the script) shows the cascade tamed: Akiba from 2m reaches 32m at 11.6s (vs the claimed pre-fix 1.7s) and stalls at 60m, max non-special growth x1.47 per held second, T5 entry 60m->goal scale ~7.8s (inside the finding's own 8-25s target edge), and the landmark chain cannot reach the 420m goal alone. The finding's load-bearing claims about current code ('absorb.js applies flat K=10 to every absorb', 'dressing growth stays at K=10', 'pacing authored via density alone') are false in the present tree. Refuted on grounds of being unreal as an open defect — already fixed in precisely the suggested shape (with retuned parameters: GROWTH_NORM_POW 0.65 vs suggested 0.45; bands 2-3 at 0.3 vs suggested 0.45). Residual risk is limited to empirical confirmation of the new constants, which the spec already mandates as the binding Phase-3 >=3-playthrough retune.

### ✗ Landmark ladder chains: from Tokyo Dome (84.6m) the goal (420m) is reachable with ZERO filler in 7 landmark absorbs and ~4s of travel — T5/T6 bands are skippable（pacing）

The finding is REFUTED against the current code: it describes the pre-Phase-3 state (東京駅 dioramaR 65, 国会議事堂 75, レインボーブリッジ 90), but the shipped file already contains the finding's own suggestedFix option 1, applied verbatim.

Evidence:
1. /Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/fableDemoGame/src/config/cityMap.js lines 235-248: L7 東京駅 dioramaR = 88 (threshold 135.4m), L8 国会議事堂 = 140 (threshold 215.4m), L9 レインボーブリッジ = 150 (threshold 230.8m). The inline comments explicitly cite this exact issue: "Phase-3 ladder respace: dioramaR 65 -> 88 so the absorb threshold (135.4m) sits ABOVE the Tokyo Dome landing (~131m). Previously L6->L7->L8->L9 chained with zero filler (Dome 84.6m -> goal 420m in 7 landmark absorbs + ~4s travel, skipping T5/T6 entirely)."
2. Running scripts/pacing-model.mjs section C (the model the finding cites) now prints: Dome 84.6 -> 131.4m, then "東京駅 NOT yet: need 135m have 131m — filler required" ... "goal 420m: not reached by landmarks alone (chain broken — GOOD)". The zero-filler chain no longer exists.
3. The numbers check out independently: jump factor cbrt(1 + 10*0.65^3) = 1.5534; Dome landing 84.6*1.5534 = 131.4 < 135.4 (Station threshold); Station landing 135.4*1.5534 = 210.3 < 215.4 (Diet threshold). Two breaks in the mid-ladder. Chaining from the Diet landing (334.5m) through bridge spans/tower is the spec-intended finale ramp (BINDING per cityMap.js comment and tuning.js lines 324-336), not a defect.
4. The complementary mitigation also landed: tuning.js growthKForObjR (lines 337-349) tapers K from 10 to a floor of 2 for non-curated objects > 0.1m real radius, and physics/absorb.js _absorbOne (line ~245) exempts only landmark/collectible slots — so the filler growth between ladder rungs is now slow T5/T6 play, not a parallel cascade.
5. validateCityMap's ladder assert (strictly increasing thresholds) passes with the new values, and a claude-mem observation dated Jun 11 2026 11:46 records "Phase-3 pacing changes validated successfully".

Residual (why not "no issue at all"): the anti-chain margins are thin — 3.0% at Dome->Station (131.4 vs 135.4) and 2.4% at Station->Diet (210.3 vs 215.4). A player who arrives at Tokyo Dome with r >= ~89.3m (5.6% above its 84.6m threshold) re-opens the full chain. However, reaching 89.3m from the 渋谷109 landing (66.9m) itself requires ~x1.33 radius of T5/T6 filler growth under the K-floor-2 taper, so even the worst case no longer "skips T5/T6 entirely", and a fast optimal route is consistent with the spec's stated optimal target (3:30-4:00, RANK_S_S=240). This margin question is a tuning nuance for the binding >=3-playthrough Phase-3 retune, not the reported defect. Severity adjusted to low accordingly.

### ✗ Zone-mask starvation deserts: GENERAL_FILL_MASK allows only bands 4/5, so tier-2/3 players outside the four authored rects find literally nothing absorbable; T4 entry has a map-wide dead window 12->15.4m（pacing）

The finding's raw facts are accurate (GENERAL_FILL_MASK = bandMask(4,5)|T6 at cityMap.js:179; Akiba {2,3}, Ueno {3,4}; konbini 10m, department_store 70m nominal), but the defect claim fails on four grounds. (1) Spec-compliant by design: DESIGN-V3.md line 73 authors "general fill everywhere else inside bounds {4,5}" verbatim, and line 79 makes the deserts the intended size-gating ("size-gating is purely the landmark threshold ladder + zone bands; Travel budget: tier-N content within reach of speed 8.5r/s — T2 within 300m, T3 within 800m..."); line 344 assigns zone-mask tuning to the binding Phase-3 empirical retune, which is present in the code (DENSITY_K_BY_BAND, growthKForObjR, landmark ladder respace) and was validated with >=3 playthroughs hitting the 5:30-6:30 target (Jun-11 observation: Phase-3 pacing validated). (2) The numbers ignore radiusJitter (spawner.js:1102): konbini/gas_tank 10m±15% are absorbable from 13.08m, not 15.4m; department_store 70m±15% from 91.5m, not 107.7m — the claimed windows shrink materially. (3) It ignores N-1 leftover-band supply (spawner.js leftover machinery): players reach r=12 inside the band-3 districts that fed them, where band-3 leftovers (absorbable from ~10.3m) bridge T4 entry; for T5, band 4 IS in general fill, so leftovers exist essentially map-wide, plus Tokyo Dome absorbable at 84.6m sits in a {4,5} zone. (4) The "0% growth for 300+s" model assumes a stationary ball; speed scales 8.5r/s (~100m/s at r=12), making any general-fill pocket seconds from supply, with no softlock topology. The Ueno だるま/パンダ detour is optional album content that never despawns, is collectible at any r>=0.77m including the natural T3 visit (Ueno is the authored T3 district), and the Akiba/Ueno rects are contiguous — the early-detour lull is a bounded ~20-40s side-quest cost, not a trap. Residual kernel (thin 12-13.1m general-fill supply, optional-detour lull) is low-severity polish already within Phase-3's validated tuning envelope, not the high-severity bimodal-pacing defect claimed.

### ✗ Toast / collect-popup overlap at 390px is geometrically unavoidable with the current anchors: 58vw cap + 198px card + margins = 432px > 390px viewport（pacing）

The finding is stale: it describes the pre-fix code, and the repository already implements the finding's own suggested fix. In current index.html, #collect-popup anchors at top: calc(118px + var(--s-t)) (line 293), a second band row below the toast (toast top 74px, ~34px tall, bottom ~108px — clears 118px), not at the same 74px anchor the finding cites. The quoted 'no overlap by construction' comment at index.html:277 does not exist; the actual comment (lines 286-289) documents the 390px/58vw overlap as the rationale for the row layout. Donack moved to its own third row at top 206px (lines 228-232) with the bubble-width collision explicitly addressed, and the landscape media query (lines 744-752) gives explicit non-colliding anchors (toast 50 / popup 90 / Donack 96, bubble capped min(55vw,300px)). The hud.js 58vw JS cap is removed — _onCollect (hud.js:597-615) contains no maxWidth write and its inline comment states the cap is retired. Claude-mem observations from Jun 11 11:54-11:55am confirm these fixes landed today. The only residual defect is a stale JSDoc line at hud.js:594 still claiming 'the toast max-width is capped to 58vw (frozen one-liner)', contradicting the implementation — a documentation-only cleanup, hence adjusted severity low.

### ✗ Street/gutter clusters use square jitter (spread applied to BOTH axes): 50% of the 中央通り dressing lands outside the 13.4m strip, including vending machines BEHIND the shop（pacing）

The finding describes a real historical defect, but it is already fixed in the current code and therefore does not hold. (1) Every STREET_CLUSTERS row (cityMap.js:377-399) and GUTTER_CLUSTERS row (407-430) now carries a rect clamp — line 378 is literally the finding's own suggested fix, vending machines clamped to x[15.5,17.8], utility poles to x[16.9,17.6] z[-180,-60]. When rect is set, expandClusters ignores spread entirely (lines 482-485), so no square jitter applies. (2) The comment at lines 372-376 explicitly documents this as the applied "Phase-3 fix", quoting the same ~50%-outside-strip symptom the finding reports — the finding was generated against a stale snapshot (memory observation from today 11:46 confirms Phase-3 pacing changes validated). (3) Executing the module: 137/138 exterior corridor placements land inside the x[4.6,18] strip; the lone outlier at (1.6,-0.8) is archetypeCode 92, the release-gated shop shell building itself, positioned by design — zero dressing in the west backlot. validateCityMap() passes and the growth-chain table shows 43-70 absorbables within 150m for exit r 0.1-0.4m, further reinforced by the new Phase-3 exit-lane litter carpet (lines 418-429). (4) Remaining null-rect rows (yatai singletons, DISTRICT_CLUSTERS area scatters at distant districts) are intentional and outside the finding's scope. No code change is needed; the claimed defect is unreal in the current tree.

### ✗ goal_call (P3) always interrupts the Tokyo Tower trivia (P3) on the same absorb: the 262->406m jump crosses GOAL_CALL_RADIUS_M=380 instantly, so the marquee 333m line is never readable（pacing）

The finding accurately described a real bug, but it is already fixed in the current tree, so it does not hold against HEAD. Evidence: (1) The premise verifies — tokyo_tower radiusNominal 170 is documented "absorbable @ 262m" (catalog.js:2589), GOAL_CALL_RADIUS_M=380 (tuning.js:34), and finale.js:264 emits GOAL_CALL in the same frame as the tower LANDMARK absorb; lm_tower and goal_call are both P3. (2) However, donack.js _trigger (lines 460-475) now implements exactly the suggested fix: goal_call arriving while a P3 bubble is live is QUEUED (queue-of-1) instead of interrupting, with an in-code comment describing this precise 262->406m scenario; on trivia dismiss, _flushPending + gapOk(3)=true shows goal_call immediately, so the 333m trivia gets its full 6s. (3) The id 3/9 col_generic sharing is also fixed: donackLines.js appends col_golden_objet ("APPENDED (Phase-3 fix)") and COLLECT_LINE_IDS[9] maps to it. (4) The tier6 sub-issue no longer manifests: in the tower-jump frame tier6 (P2) lands in the queue-of-1 and is replaced by the queued goal_call (P3), so it is silently discarded rather than displayed nonsensically — at worst a dead line of copy. Claude-mem observations from 11:55a today ("Fixed Donack goal_call interrupt blanking Tokyo Tower trivia", "Added dedicated Donack line for collectible ID 9") confirm the finding was remediated before this verification. The finding's cited code (donack.js:458-460 "always shows, interrupts") no longer matches the file. Residual severity is low: tier6 is effectively unreachable vestigial copy with no user-visible symptom.

### ✗ Rank ladder S240/A330/B450/C600 is currently dead: modeled optimal clear is ~2 minutes and even a naive corridor run finishes well under RANK_S_S — thresholds are fine only AFTER the growth fix lands（pacing）

The finding's headline claim — "rank ladder S240/A330/B450/C600 is currently dead: modeled optimal clear ~2 minutes, naive corridor run finishes well under RANK_S_S" — does not hold against the current code. The fix set it was explicitly contingent on ("thresholds are fine only AFTER the growth fix lands") has already landed in the shipped game: growthKForObjR normalization (tuning.js:337-348) is used in the real absorb path (src/physics/absorb.js:245) and DENSITY_K_BY_BAND per-band density (tuning.js:127) in the real spawner (src/world/spawner.js:801,804). Re-running scripts/pacing-model.mjs now shows: the naive hold-W corridor never grows at all (ends r=0.4m @ 502.9s — it cannot finish under 240s, directly contradicting the finding); the Akiba cascade is 2->32m in 11.6s (not the quoted 1.7s) capped at x1.47 non-special growth/s and stalling at ~60m in-region; per the finding's OWN post-fix model the existing thresholds land exactly on the design targets (typical 5:30-6:30 / optimal ~3:40, DESIGN-V3.md line 103). Additionally there is no defect in the cited file: runStats.js (lines 267-275) implements rank/time-bonus exactly per spec, and the finding's suggestedFix reduces to "change nothing and run the Phase-3 >=3-playthrough empirical retune" — which DESIGN-V3.md already mandates as binding (lines 103, 316, 344). The finding was a derivative restatement of the separately-filed critical growth-cascade finding plus a recommendation to preserve the status quo; as a standalone defect it is stale and unactionable. Residual (non-rescuing) note: the model still shows a T5-entry cascade (60->600m in ~7.8s), but that belongs to the growth/density finding's domain, and the Phase-3 playthrough verification remains the spec-mandated check on actual clear times.

### ✗ Shop-exit hold-W actually STALLS before it runs away: a straight eastward exit outruns its own growth chain (r stays 0.42m to the map edge); the cascade only ignites for players who weave — pacing is whiplash, not a single curve（pacing）

Stale finding: its exact suggested fix is already in the tree. cityMap.js GUTTER_CLUSTERS lines 418-429 contain a "Phase-3 exit-lane carpet (shop-exit whiplash fix)" whose comment restates this finding verbatim and implements the proposed remedy exactly (0.025-0.30m litter rows clamped to x[5.2,17.6]/z[-40,40] plus a trash_can cluster straddling z=0). Headless re-run of the model on current data: 11 absorbables at r=0.42 within |z|<=8 in the first 25m past the gate (vs the finding's "8 total to the map edge"), and validateCityMap() passes with 43-70 absorbables within 150m across exit radii 0.10-0.4. The paired runaway fix (growthKForObjR normalization, tuning.js:345; DENSITY_K_BY_BAND; landmark ladder respace) is also landed, so the "whiplash" no longer has either half. Only residual: a strictly straight |z|<=1.2 corridor still meets ~1 item — but the carpet is within easy steering view, matching the design intent ("without backtracking").

### ✗ @property --gauge with inherits:false makes the dash ring permanently empty in every modern browser（mobile）

The finding claims index.html registers `@property --gauge { ... inherits: false; ... }`, making the ::before-painted dash ring permanently 0deg in all @property-supporting browsers. The actual code at /Volumes/AIWorkSSD/AIWorkSpace/github/aieo-product/fableDemoGame/index.html:39-43 reads `inherits: true;` — and a comment at lines 35-38 explicitly documents why ("inherits MUST be true: the var is written on #dash-button but painted by #dash-button::before — a registered non-inheriting property would resolve to initial-value 0deg on the pseudo-element (empty ring forever in every @property-supporting browser)"). Verified this is the ONLY registration: no other `@property` rule and no CSS.registerProperty call anywhere in src/ or index.html (other grep hits are JSDoc @property annotations). With inherits:true, the value hud.js _writeGauge (src/ui/hud.js:727-733) sets on #dash-button inherits into ::before, the conic-gradient at index.html:407 paints the arc, and the registered-property transition at index.html:410 works. The finding's CSS theory is correct in the abstract, but its factual premise (inherits: false) is false against the current code — the bug does not exist as described.

### ✗ .overlay justify-content:center + overflow-y:auto top-clips the result screen at 360x640 and the title screen at 844x390 with no way to scroll up（mobile）

The finding misquotes the code. It claims `.overlay { ... justify-content: center; overflow-y: auto; }` at index.html:467-479, but the actual `.overlay` rule (index.html:478-497) uses `justify-content: flex-start` and already implements the exact overflow-safe pattern the finding proposes as the fix: `.overlay > :first-child { margin-top: auto; }` and `.overlay > :last-child { margin-bottom: auto; }` (lines 496-497), with an explanatory comment at lines 482-485 explicitly calling out the flexbox-centering clipping hazard ("justify-content:center clips overflowing column content ABOVE the scrollport... the margin:auto pair below absorbs free space when short (= centered) and collapses to 0 when tall (= fully scrollable)"). Lines 467-479 cited by the finding actually contain the #flash-overlay rule and keyframes, not the .overlay rule.

I verified no later rule undoes the pattern: the only other `justify-content: center` declarations apply to `.pill` (line 107), `.key-hints` (543), `#result-rank-wrap`-area rules (594/607/670), and `.result-buttons` (709) — none target `.overlay`. None of the three media queries (max-width:360px at 686, landscape max-height:480px at 736, min-width:768px at 760) touch `.overlay`'s justify-content or the margin-auto pair. The margin-auto pattern also remains effective in the DOM: both #title-overlay (first child h1, last child p.seed-line, lines 898-913) and #win-overlay (first child h1, last child .result-buttons, lines 919-954) keep their first/last children permanently in flow — the `.staged` reveal only animates opacity (does not remove elements from layout), and the `.hidden` #title-best-line is a middle child. So when content exceeds the viewport at 360x640 or 844x390, the auto margins collapse to 0, content aligns to flex-start, and `overflow-y: auto` makes everything reachable by scrolling. The described top-clipping cannot occur with this CSS.

The finding's content-height arithmetic may or may not be accurate, but it is moot: even if the result stack overflows 640px, it scrolls from the top rather than clipping. The reported bug does not exist in the current code — the suggested fix is verbatim already present.

### ✗ Mid-band overlap matrix: #hud-toast x #collect-popup x #donack-bubble collide at 360-430px portrait (and an 8px strip in landscape)（mobile）

All three claimed collisions describe code that does not exist in the current tree — the finding's own suggestedFix is already applied, nearly verbatim. (1) #collect-popup is NOT at calc(74px + --s-t); it sits on its own second row at top: calc(118px + var(--s-t)) (index.html:293), below the toast band (74..~108px), making toast/popup overlap impossible at any width; the 58vw JS cap the finding says to delete is already gone from src/ui/hud.js _onCollect (lines 597-615 contain only a comment noting the cap "is retired"; grep confirms no style.maxWidth anywhere in the UI modules). (2) #donack-root is at top: calc(206px + var(--s-t)) (index.html:232), a third exclusive row below the popup band (ends ~184px) — the bubble cannot reach the popup's vertical extent. (3) The landscape block (index.html:736-753) already moves #collect-popup to width:180px; top: calc(90px + var(--s-t)) — below the toast band ending ~84px — and caps #donack-bubble to min(55vw,300px) with a 48px avatar, clearing the popup column on any realistic landscape width (16px gap at 568px). The finding's cited line numbers (191, 282, 246, 277, 726) match nothing in the current file, and the CSS comments at index.html:228-231 and :286-289 quote the finding's own overlap arithmetic — it was generated against a pre-fix snapshot and the remediation has since landed. Residual nits only: a stale JSDoc at hud.js:593-594 still claims the 58vw cap exists (contradicted by the code/comment directly below), and a hypothetical 480px-wide landscape viewport (extinct, iPhone-4-era) could still produce a ~36px Donack/popup horizontal overlap. Neither is user-visible on supported hardware, so adjusted severity is low.

### ✗ #goal-arrow exclusion zones ignore safe-area insets — arrow overlaps the top bar on notched phones and the dash button at bottom-right（mobile）

The finding describes code that is not what exists in the repo — every specific claim is contradicted by the current hud.js, and the file already contains exactly the suggested fix:

1. Claim "ARROW_EXCLUDE_BOTTOM_PX=100" — false. hud.js:93 has `const ARROW_EXCLUDE_BOTTOM_PX = 110;` (the value the fix said to bump to). ARROW_EXCLUDE_TOP_PX=64 and ARROW_HALF_PX=28 are at lines 92/95.

2. Claim "_onGoalGuide clamps with raw window.innerHeight ignoring insets" — false. hud.js:556-563 clamps with `xMin = this._safeL + ARROW_HALF_PX`, `xMax = w - this._safeR - ARROW_HALF_PX`, `yMin = ARROW_EXCLUDE_TOP_PX + this._safeT + ARROW_HALF_PX`, `yMax = h - ARROW_EXCLUDE_BOTTOM_PX - this._safeB - ARROW_HALF_PX`. Both the y AND x (landscape notch) inset handling the finding asked for are present.

3. Claim "insets ignored / should be read in the constructor from --s-t/--s-b/--s-l/--s-r" — already done. _readSafeInsets() (hud.js:627-634) parses getComputedStyle(document.documentElement) values of --s-t/--s-b/--s-l/--s-r, is called in the constructor (line 159), re-run on window resize (line 162, comment explicitly notes resize covers orientation changes), and on breakpoint flips via _onBreakpointChange (line 622). index.html:49-52 defines those vars as env(safe-area-inset-*).

4. Geometry check with the actual constants: on an iPhone with safe-area-inset-top=47, yMin = 64+47+28 = 139, so the arrow top edge is at y>=111, clearing the top bar (ends ~99) and progress bar (~103..108 per the finding's own numbers). Bottom: with s-b=34, yMax = h-110-34-28 = h-172, arrow bottom edge at y<=h-144, clearing the dash button top (h-130) by 14px. No overlap remains.

The only residual nit is the stale doc comment at hud.js:535-536 ("bottom thumb strip (100px)") which still says 100 instead of 110 — a documentation inconsistency, not a behavioral bug, and the file header (lines 9-10, 88-90) correctly documents "bottom 110px + live safe-area insets". Whether the finding was generated against a stale snapshot or the fix landed after the finding was filed, against the current tree it is fully addressed and the described overlap is unreachable.

### ✗ Touch edge cases: dash-button tap triggers boost, and a finale-skip touch spawns the joystick ring over the cinematic（mobile）

Both issues are already fixed in the current code, exactly per the finding's own suggested fix. (1) Boost coupling: src/input/input.js no longer sets _touchCount from e.touches.length; _handleTouchStart (line 361) and _handleTouchEnd (line 403) use _countGameTouches (lines 329-339), which excludes touches whose target.closest('button') is non-null — #dash-button is a <button> (index.html:886) — so a dash tap cannot push _touchCount to 2 and read()'s boost (line 168) cannot fire from it. (2) Finale-skip ring: setTouchUiEnabled (input.js:347-356) gates _handleTouchStart (early return at line 360) and hides/clears the joystick when disabled; main.js wires input.setTouchUiEnabled(false) on EVT.GOAL_CONTACT (main.js:493) and re-enables on GAME_START (main.js:488). The finding's cited line numbers do not match the current file; the touch-UI gating was implemented 2026-06-11 (per project observation log), after the finding was drafted. Neither defect is reachable in the current codebase.

## 最終バリデーション

FINAL VALIDATION: GO FOR DEPLOY. Build green (755.69KB / 205.37KB gzip). Full agent-browser pass on the prod preview (port 4341) plus a dev-server probe session (4342) for sim ground truth. MOBILE: 390x844 and 360x640 each played 60s+ via synthetic touch joystick — absorbs, dash latch, Donack bubble, tier progress all live; a 250ms-interval DOM overlap monitor ran continuously across both runs and logged ZERO overlapping HUD pairs and zero offscreen elements (the one transient 'OFFSCREEN donack-avatar' tick is its slide-in animation); collect popup + rare toast force-fired at 390px: clean. Draw calls 46/72 mobile, heap delta +0KB, zero console errors. DESKTOP 1280x800: organic shop run (worst 1s growth ratio 1.292), shop->street exit observed ~1:50, all five ?at= teleports work, finale e2e green — goal call at 420m with Donack callout + edge arrow, arrow-guided Skytree contact, confetti night-view cinematic (~6-10s, skippable), result with staged reveal, rank, NEW RECORD, 12/12 collection grid; X intent verified end-to-end: https://twitter.com/intent/tweet with text (time/rank/score/collection), url=fable-katamari.pages.dev, hashtags=FableKatamari (navigates to X login redirect as expected); ROLL AGAIN drops straight into a clean fresh 2cm shop run (second run played). PACING RUNAWAY RETEST (the critical Phase-3 item): GONE — ueno corridor held-W 4.00m->6.72m over 35s (worst 1s 1.21x), marunouchi 40->45.6m/10s, tower steady-state worst 1s 1.18x at 200ms sampling; the only >1.5x jumps are t=0 teleport spawn-overlap multi-absorbs (dev-key artifact, not reachable organically). New rank constants (290/400/540/720, time bonus 290/720) confirmed in tuning.js. RESCALE PIXEL-IDENTITY: KeyR on dev build (worldScale 0.04->0.2, rescaleCount 1): of 1280x720 pixels only 407 differ and ALL sit in the top-60px HUD strip (ticking timer digits) — the world render is byte-identical. Donack cadence measured live: 20-28s gaps, no spam (some line repetition within a 4-min stint — cosmetic). Draw calls peak 55/72 (ueno), tris well under budget, console: zero errors/warnings everywhere. One regression found and fixed (desktop collect-popup under mute button, CSS one-liner, rebuilt + verified); five screenshots written to docs/screenshots/v3-final-{mobile,mobile-small,street,finale,result}.png. Both servers killed, browser session closed.

**残存懸念**:

- donack-bubble ~ goal-arrow transient overlap on desktop when the goal sits at the bottom-left screen edge (observed ~30 monitor ticks at the tower band); the arrow is dynamically edge-positioned so a CSS fix is not obvious — cosmetic, never blocks input (both pointer-events:none)
- At 1280x800 the result overlay's POST/RESTART buttons start at y=778 (mostly below the fold); the overlay scrolls by design (margin:auto pattern) so users can reach them, but coordinate-based automation missed them twice — consider tightening result vertical gaps at short viewports
- Early shop exit is possible at ~3.5cm (open front, no gate) into a street band whose growth chain is validated only for exit radii >= 0.10m; a tiny ball east of the Chuo-dori strip finds barren ground for minutes (Donack does coach to stay small) — opening-minute pacing is the weakest stretch of the run
- ?at= dev teleports are parsed in the production build; drop points can spawn overlapping curated clusters (tower: 250m->470m multi-absorb at t=0) and ?at=goal&r=500 spawns already in Skytree contact, ending a run at 00:00.0 and persisting it as the localStorage best record — harmless for normal players but a public-URL cheese vector worth a guard before sharing
- Boot/validator console.log tables (cityMap growth-chain, geometryFactory, boot line) print in the production build — log noise, zero errors/warnings
- BatchedMesh extraPools rendering confirmed working in headless Chrome (collectible cards, landmark absorbs, 12/12 collection), but the WEBGL_multi_draw-absent fallback path remains unmeasured on real low-end devices

## メインループによる追加修正（デプロイ直前）

- バリデータ指摘「`?at=`/`?r=` dev URLが本番で自己ベストを汚染できる（00:00.0クリアが記録される）」に対し、RunStats に `markDevRun()` を追加。dev start を適用したセッションでは記録の永続化と NEW RECORD 表示を抑止（dev URL自体は本番でもデバッグ用に動作継続）。