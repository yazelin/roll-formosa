# Phase 6 作業ログ: v2実装（ワークフロー `katamari-v2-implement`）

**手法**: v1と同じ「契約凍結→並列ストリーム→統合」だが、v2は既存コードへの差分実装。Phase 0が型/イベント/定数/DOM/main骨格を凍結し、5ストリームが互いのファイルに触れず並列実装、統合エージェントが設計指定の順序（D→C→E→B→A）で配線しブラウザ実機でフィナーレ完走まで検証。

**実行実績**: エージェント7体 / 約1,196,041トークン / 約52分

**結果**: ビルド成功 / ブラウザ検証合格 / フィナーレ3回完走検証 / XポストURL検証済み / ドローコール 46/60 / バンドル 645.31 KB (raw)

## Stage 1: 契約凍結（Phase 0）

**変更ファイル**: `src/types.js`, `src/core/events.js`, `src/config/tuning.js`, `src/config/tiers.js`, `index.html`, `src/main.js`, `docs/DESIGN.md`

**アプローチ**:

Phase 0 contract layer implemented as a pure delta on the working v1 files, every name taken verbatim from DESIGN-V2.md interfaces. types.js gained all v2 typedefs (Intent.dash, BallState.dashGauge01/dashTimer, GrowEvent.dashGauge01, AbsorbEvent.rare, 7 Tier sky params, Dash/DashReady/Score/Time/MoonCall/MoonGuide/MoonContact/Goal/MuteRequest/MuteChanged events, BestRecord) plus updated stride comments (60 = ARCH_PER_TIER 10 x 6). events.js gained the 10 EVT names ('dash','dashReady','score','time','moonCall','moonGuide','moonContact','goal','ui:muteRequest','muteChanged'), matching pre-allocated PAYLOADS entries, rare:false on PAYLOADS.absorb, dashGauge01:1 on PAYLOADS.grow, and the binding header notes (ABSORB order main->runStats->sfx/effects/hud; game:win emitted by main at finale 'done'; goal must be copied). tuning.js gained every constant in the design tuning section grouped into v2 Moon/finale, Dash, Score/rank, Rares, Persistence sections; WIN_RADIUS_M is @deprecated but still exported (main.js boot log imports it); ALIVE_SCENERY_BUDGET deleted (verified never imported); SCENERY_OBJECTS_PER_CHUNK 8->10 with peak-population comment; DRAW_CALL_CAP 60 with the full 40+8+6+1=56 ledger comment. tiers.js exports ARCH_PER_TIER=10, appends the 12 frozen landmark ids (soda_bottle/desk_globe, bookshelf/grandfather_clock, utility_pole/torii, pylon/giant_tree, castle/pagoda, mountain/skytree) as slots 8/9, adds per-tier sunDir/sunIntensity/moonDir/moonAngSize([0.018..0.062])/starIntensity([0,0,0,0,0.15,0.5])/cloudDensity/cloudHex (moonDirs all in one sky quadrant with elevation >=0.4 rad so crossfade lerps can never dip below the 0.15 floor), and DEV asserts: 10 ids/tier, 60 unique, post-normalization moonDir elevation >= MOON_DIR_MIN_ELEV, moonAngSize strictly increasing, sky scalar ranges. index.html adds all v2 DOM (#timer, #score-panel/#score-value/#score-float-layer, #dash-gauge/#dash-gauge-fill, #dash-button, #mute-button, #hud-toast, #moon-arrow, #vignette, #flash-overlay, staged result overlay with #result-time/-score/-size/-absorbed/-rares/-rank, #new-record-badge, #result-best, #result-seed, #post-x-button, #title-best-line) and CSS keyframes (result-reveal stagger gated by #win-overlay.staged so the v1 non-staged path stays fully visible, rank-stamp scale-3->1 bounce + flash, badge-pop, toast, gauge-flash, contact-flash 0.12s-in/0.45s-out, score-fly) while preserving every v1 id. main.js gained GameState.FINALE, no-op finale/runStats stub objects (inputLocked/cameraOwned always false => v1 behavior byte-identical), the binding v2 frame order (gated absorb.resolve/spawner.update/maybeRebase, finale.update at 4.5 with the PLAYING->FINALE transition, cameraOwned gate at 6, runStats.addSimTime at 6.5), the idle-render gate widened to exclude FINALE, resetWorld += finale.reset/runStats.reset, and commented V2-WIRE blocks for every integration point (imports, MoonView/Finale/RunStats/Bgm/Backdrop construction, mute ownership setMutedAll, GAME_WIN emission, setRareProvider, backdrop.update). DESIGN.md got the v2 chapter: allocation-law bounded WebAudio exemption (<=60 nodes/s), draw-call ledger 60/56, event-contract pointer, fog:false moon exemption, sim-time rank clock, seed incompatibility, reset-ownership table, frame-order delta. All files pass node --input-type=module --check.

**主要な意思決定**:

- Stub objects over null checks: main.js uses const finale = {state:'idle', inputLocked:false, cameraOwned:false, update(){}, reset(){}} and runStats = {addSimTime(){}, reset(){}} so the v2 gates/call sites are REAL code already in the binding order — the integrator only swaps the stub declarations for real constructions.
- Idle-render gate changed to `state !== PLAYING && state !== FINALE` — required so the sim keeps stepping through the finale cinematic; inert in v1 mode since FINALE is unreachable with the stub.
- Kept WIN_RADIUS_M exported with @deprecated JSDoc (main.js boot log still imports it); deletion is an integration task per the design.
- #win-overlay staged reveal is opt-in via the .staged class: without it (v1 screens.js path) all result rows are visible immediately, so the current GAME_WIN flow still renders a sane win screen; #win-size and #win-seed kept (nested inside #result-size / #result-seed).
- moonDir values chosen with elevation asin(y) >= ~0.4 rad for all 6 tiers and all dirs in the same sky quadrant (-x,+y,-z), making the crossfade-lerp elevation floor (0.15 rad) trivially safe; assert checks POST-normalization elevation per the design's belt-and-suspenders note.
- PAYLOADS.grow.dashGauge01 initialized to 1 (gauge starts full per design); PAYLOADS.absorb.rare to false.
- Backdrop/Bgm constants (BACKDROP_*, BPM etc.) deliberately NOT in tuning.js — design marks them module-local.
- Mute button placed top-right under the absorbed counter (top:100px right:16px) to satisfy 'top-right' without disturbing the v1 absorbed-counter; dash gauge placed above the dash button bottom-right rather than crowding the center progress bar.
- tiers.js assert ARCH_PER_TIER === 10 added as a freeze guard; moonAngSize strictly-increasing assert added (the 'moon approaches' invariant).

**設計からの逸脱**:

- index.html: added #title-best-line/#title-best-value (hidden by default) to the title overlay for the '自己ベスト via RunStats.loadBest()' requirement — the design assigns the behavior to Stream E but names no DOM id; these two are now the frozen ids.
- index.html: result rows use wrapper ids #result-row-time/-score/-size and #result-detail in addition to the spec'd value ids; .result-buttons wraps #post-x-button + #restart-button — purely structural, all spec'd ids exist verbatim.
- index.html: added Space (Dash) and M (Mute) key hints to the title screen and changed title/subtitle copy from '500m' to moon-themed wording — cosmetic, consistent with the v2 goal change.
- main.js header comment: documents the v2 binding order in full; the RunStats construction site carries a note telling the integrator to verify sfx/effects ABSORB-subscription positions when wiring the binding order (v1 constructs Sfx/Effects before Hud; runStats must subscribe ABSORB after main's attach-handler and the stub placement preserves a correct slot).

## Stage 2: 並列開発ストリーム

### ストリーム A: 月フィナーレ（finale/moon/scaleManager/cameraRig/effects）

**ファイル**: `src/game/finale.js`, `src/render/moon.js`, `src/world/scaleManager.js`, `src/render/cameraRig.js`, `src/render/effects.js`

**アプローチ**:

finale.js implements the nine-state machine (idle/called/descent/landed/contact/merge/ascension/afterglow/done) as a threshold ladder plus a per-state switch driven by update(frameDt, ball); trueRadius is computed as ball.radiusSim * scaleMgr.worldScale each frame. The ONLY rescale/rebase-sensitive state is the _simCache struct {startX/Y/Z, landX/Y/Z, moonR, mergeFromX/Y/Z, ascendBaseY}; finale subscribes EVT.RESCALE (every field *= S) and EVT.REBASE (X/Z fields -= sx/sz) itself, and every moon pose / camera target is re-derived per frame from the cache, verified by mid-descent-rescale and mid-landed-rebase headless tests. Descent freezes moonR = 2.6r, picks the landing point along the velocity direction (camera-forward fallback below 0.5*speedCap), and computes the angular-matched start pose cameraPos + getMoonDirWorld * (moonR / tan(TIERS[tier].moonAngSize)) — test confirms the T5 distance is exactly 41.9r — while driving env.setSkyMoonFade 1->0 over 2.0s; the moon lerps easeInOutCubic over 6.0s plus a 0.4s settle bounce. LANDED applies the horizontal soft-magnet bias and the 3D render-frame contact test (dist <= ballR + moonR*0.85); MOON_GUIDE is emitted at 10Hz via manual view-space + projection-matrix transforms (behind-camera mirroring, y01=0 at top). CONTACT latches inputLocked/cameraOwned, records mergeFrom, emits guide-off then MOON_CONTACT, and calls cameraRig.beginCinematic(); merge lerps ball.pos into the moon (ball view hidden at 0.6s, 1.06 scale pop); ascension rises quad-ease-in 40r with beginNightFade(5.0) + effects.moonBurst(); afterglow breathes the glow at 0.5Hz then 'done'. The hidden ball is parked on the moon center during ascension/afterglow so the no-arg moonBurst fountain and blob shadow track the ascent. moon.js bakes 26 seeded craters (mulberry32(seed^0x4d4f4f4e), position-hash speckle, rim highlights) into vertex colors on a 1280-tri icosphere with fog:false Lambert body + additive BackSide glow shell (2 draw calls). cameraRig gains the EVT.DASH +8deg sin-envelope FOV kick and beginCinematic/cinematicUpdate/endCinematic reusing the existing springVec3 state (no snap at handoff); scaleManager loses the win latch entirely (no _won field), uses MOON_GOAL_RADIUS_M for last-tier grow progress, and copies dashGauge01 onto the grow payload (defaults 1 pre-Stream-D); effects gains the dash 10-line burst, setRareProvider golden twinkles (~3/s per rare within FOG_FAR_K range, pre-bound zero-alloc callback), the moonBurst fountain (4.5s, 26/s, pops pool) and dustRing (28 quads, ring pool) — still exactly 3 draw calls. Zero per-frame allocation throughout (module scratch Vector3s, reused PAYLOADS).

**主要な意思決定**:

- IcosahedronGeometry(1, 7) not (1, 3): three.js PolyhedronGeometry detail d gives (d+1)^2 tris/face, so detail 7 = the budgeted 1280 tris ('icosphere detail 3' in the design speaks classic 4^k subdivision); the tri budget is the binding number — documented in moon.js and asserted by the smoke test
- Effects reach finale via an optional 8th constructor arg + finale.setEffects(effects) setter, because the frozen 7-arg constructor has no effects channel yet the design requires finale to call effects.moonBurst()/dust ring; all effects calls are typeof-guarded no-ops when absent
- dustRing(cx, cy, cz, radiusSim) signature chosen by me (design only said 'LANDED dust ring helper'); finale calls effects.dustRing(landX, 0, landZ, moonR) once on entering LANDED
- During ASCENSION/AFTERGLOW finale parks the hidden ball.pos on the moon center (vel zeroed) so the no-arg moonBurst fountain and the blob shadow track the rising moon; intent is already zeroed by main so nothing fights
- Cinematic 'back' direction = horizontal normalize(mergeFrom - land) from _simCache (rescale-covered), so camera targets are pure per-frame functions with zero cached camera state; FOV eases 60->52 over MERGE+ASCEND via easeInOutCubic of a contact-relative clock
- cameraRig.update() AND updateIdle() early-return while the cinematic latch is set — keeps main's WIN-state idle-orbit branch from yanking the final moon shot from behind the result screen; reset()/endCinematic() clear it
- env v2 methods (getMoonDirWorld/setSkyMoonFade/setSkyMoonPulse/beginNightFade), cameraRig cinematic methods and ballView.setVisible are typeof-guarded with one-time DEV warns so the finale runs headless and pre-Stream-B/C bring-up doesn't crash; getMoonDirWorld falls back to the tier's normalized moonDir
- MOON_FADE_S=2.0 is finale-module-local (matches the design's '2.0s crossfade') rather than reusing PALETTE_FADE_S or touching frozen tuning.js; same for SETTLE_S=0.4, CINE_BACK_K=14, CINE_UP_K=4, CINE_FOV_END=52, BALL_HIDE_AT_S=0.6, MERGE_POP_AMP=0.06
- DEV watchdog covers BOTH 'idle' and 'called' (design text says idle; called is the same dead-trigger risk) — console.error + forced _beginDescent at tr > 1.2*MOON_GOAL_RADIUS_M
- 'contact' is one observable frame (events + latches fire on the detection frame inside _updateLanded), then merge begins the next update — the state getter reports every listed state name
- Dash FOV kick uses the tierUp clock+sin(pi*t/T) idiom with DASH_FOV_BONUS over DASH_DURATION_S (peaks +8deg at 0.4s, returns to base), additive with the existing kick/speed bonuses
- scaleManager grow payload: PAYLOADS.grow.dashGauge01 = (ball.dashGauge01 >= 0 ? it : 1) tolerates a pre-Stream-D BallState without the field
- MOON_GUIDE projection uses camera.matrixWorldInverse from the last render (one frame stale, fine at 10Hz); behind-camera points are sign-mirrored so the HUD edge arrow points the short way; emitted guide-off precedes MOON_CONTACT on the contact frame

**逸脱**:

- Moon icosphere built with detail parameter 7 (not the literal 'detail 3') to hit the design's binding ~1280-tri budget — three.js detail semantics differ from classic icosphere subdivision levels
- Finale constructor extended with an OPTIONAL trailing 8th parameter `effects = null` plus a setEffects(effects) method — the frozen interface listed 7 args and no effects path, but the moonEnding section requires finale-triggered effects.moonBurst() and the LANDED dust ring; omitting the arg matches the frozen signature exactly
- DEV watchdog triggers from 'called' as well as 'idle' (design text says 'while still idle') — strictly safer, DEV-only
- effects.dustRing(cx, cy, cz, radiusSim) public helper added to effects.js (design named the feature but froze no signature)
- The 2.0s sky-moon crossfade constant lives as finale-local MOON_FADE_S (tuning.js is frozen and has no such constant; design states 2.0s inline)

### ストリーム B: 背景（environment空シェーダv2/backdrop遠景）

**ファイル**: `src/render/environment.js`, `src/render/backdrop.js`

**アプローチ**:

environment.js: rewrote the sky-dome fragment shader (same mesh, still 1 draw call) layered as gradient -> hashed twinkling stars (floor(dir*56) grid hash, per-star twinkle speed/phase from uTime, gated by uStarIntensity and an above-horizon smoothstep) -> 2-octave analytic value-noise clouds on the dir.xz/max(dir.y,0.05) projection drifting by uTime*uCloudDrift (density-gated coverage, uCloudColor) -> sun disc + dual pow glare lobes scaled by uSunIntensity (disc fattens slightly with intensity for the T4 golden-hour sun) -> acos-free moon disc (smoothstep on dot vs cos(uMoonAngSize)) with 3 crater dots in tangent-basis disc-local coords and an exp halo * uMoonGlow, both scaled by uMoonFade -> horizon fog melt LAST (seamlessness law). All features are pure functions of view direction + an accumulating uTime seconds clock, so rescale/rebase invariance is structural; rescale()/rebase() are untouched. Palettes are built from the extended tiers.js params (sunDir/moonDir normalized to Vector3 at boot) plus an env-local NIGHT palette pushed as index 6 (sky 0x101030/0x06061a, fog 0x1a1838, stars 1.0, sun/clouds off); all v2 scalars/dirs/cloud color ride the existing _toIndex crossfade via a generalized _beginFade(index, duration) — startPaletteFade uses PALETTE_FADE_S, beginNightFade(seconds) targets index 6 with a custom duration, and direction lerps re-normalize per frame. setSkyMoonFade/setSkyMoonPulse (0.5Hz cosine-ramp breathing starting at glow 1.0, snap-free) and getMoonDirWorld(out) with an exact unit-vector MOON_DIR_MIN_ELEV elevation clamp complete the finale API; setTierPaletteImmediate snaps everything, cancels any in-flight (night) fade structurally, and resets uMoonFade=1/pulse-off/star-intensity. Zero per-frame allocation: live Colors/Vector3s are uniform refs mutated in place, scalars written into uniform .value each update. backdrop.js (new): one camera-centered 2-layer silhouette ring mesh (96 segments x 4 quad rows x 2 layers = exactly 1536 tris, 970 verts, Uint16 index), built once at boot from mulberry32(worldSeed^0x42444b) with both profiles baked as vertex attributes — aHeightA/aColorA rolling hills (integer-wavenumber sines, seamless wrap) and aHeightB/aColorB tower skyline (flat-top runs of 2-5 columns, 8% spires) — blended by uProfile01 in the vertex shader; fragment mixes vertex color toward the live fog color (copied from scene.fog.color each update, zero alloc) at BACKDROP_FOG_MIX 0.82 front / 0.90 back layer. update(dt, ball, camera) is a pure function: position=(cam.x,0,cam.z), scale=radiusSim => pixel-identical at rescale, nothing on rebase; frustumCulled=false, depthWrite=false, renderOrder -5 (after sky -10, before world 0 — the ground/world drawn later naturally hides the below-horizon portion). Subscribes TIER_UP (uProfile01 crossfade over PALETTE_FADE_S, hills T0-2 <-> skyline T3-5) and GAME_RESET (snap to hills) itself; dispose() releases everything. Verified by a headless node smoke test (since deleted): crossfade endpoints exact, night fade, reset snap, elevation clamp, tri budget, seed determinism.

**主要な意思決定**:

- Sky shader layer order: gradient -> stars -> clouds (occlude stars) -> sun (additive) -> moon disc (opaque mix, occludes stars/clouds) + halo (additive) -> horizon fog melt LAST so the fog wall always wins at the horizon (seamlessness law preserved).
- Moon halo uses exp(-(1-md)/ (3*angSize^2)) instead of pow — cheaper, scale-tracks uMoonAngSize automatically across tiers, and uMoonGlow multiplies halo only (disc stays handoff-stable while the CALLED pulse breathes).
- CALLED pulse implemented as 1 + 0.35*(1 - cos(pi*phase)) with the phase accumulator reset on setSkyMoonPulse(false) — glow always starts/returns at exactly 1.0, never snaps.
- getMoonDirWorld clamps elevation by rescaling the horizontal component to cos(MOON_DIR_MIN_ELEV) and setting y=sin(MOON_DIR_MIN_ELEV) — returns an exact unit vector at exactly the floor, not an approximate normalize-after-y-bump.
- NIGHT palette reuses T5's moonDir/moonAngSize (inert — the finale fades the sky moon to 0 before any night fade) and cloudHex = its own fog color; built through the same _buildPalette derivation as tiers so ground/line/hemi colors come out consistently dark.
- Backdrop geometry: 2 layers x 96 seg x 4 rows = 1536 tris (spec ~1.5k); back layer at radius 48 (BACKDROP_DIST_K), front at 0.8*48 with 0.62 height and darker colors; back layer fog mix 0.90 vs front 0.82 for depth haze; row 0 dips to y=-0.04*h to seal against the ground plane.
- Backdrop reads the live fog color from scene.fog.color (copy per update) instead of taking an Environment reference — robust to construction order and gets tier fades + the finale night fade (near-black silhouette) for free.
- Hills profiles use integer wavenumbers so the ring wraps seamlessly by construction; skyline wraps by duplicating column 0 into column SEG; all generation from one fixed-order mulberry32(worldSeed^0x42444b) stream => deterministic per seed (verified).
- Added two additive (non-breaking) helpers beyond the frozen interface: env.getMoonAngSize() (live blended angular radius, lets Stream A's handoff stay pop-free if DESCENT triggers mid-palette-fade) and backdrop.setProfileImmediate(tierIndex) (optional ?r= dev-start cosmetic snap).
- DoubleSide on the backdrop material (1.5k tris, negligible) instead of hand-winding inward-facing triangles.

**逸脱**:

- Added env.getMoonAngSize() — additive helper not in the frozen interface; nothing depends on it, safe to ignore.
- Added backdrop.setProfileImmediate(tierIndex) — additive helper for ?r= dev starts (without it the backdrop shows hills until the first TIER_UP on a high-tier dev start; GAME_RESET still snaps to hills per the reset-ownership table). Optional wiring next to env.setTierPaletteImmediate in main's dev-start path.
- Backdrop layer split uses module-local derived values (front radius 0.8*BACKDROP_DIST_K, front height 0.62x, back fog mix 0.90) — the spec freezes only DIST_K/HEIGHT_K/FOG_MIX; these are documented module-locals in the same spirit.

### ストリーム C: 景観+ストライド移行（catalog/objects/spawner/ball）

**ファイル**: `src/config/catalog.js`, `src/world/objects.js`, `src/world/spawner.js`, `src/render/ball.js`

**アプローチ**:

Executed the full 8->10 stride migration plus landmarks and rares across the four Stream C files. catalog.js gained the 12 frozen landmark archetypes (soda_bottle, desk_globe, bookshelf, grandfather_clock, utility_pole, torii, pylon, giant_tree, castle, pagoda, mountain, skytree) as merged vertex-colored primitives — all measured headless at 48-296 tris (cap 350), with yOffsets computed exactly from the normalized geometry minY via a node script (yOffset = -minY - 1) rather than estimated; the DEV assert now requires exactly 60 entries. objects.js exports FLAG_RARE=8, builds ARCHETYPE_ID_BY_CODE from t*ARCH_PER_TIER+i (60 codes), and DEV-asserts the table length/holes. spawner.js replaces the single _cumW table with dual cumulative weight tables (_cumW8 over slots 0-7 for j>0, _cumW10 over all 10 for j===0) fed by one raw archRoll drawn at the unchanged position — eligibility depends only on j so chunk role upgrades regenerate identically; rareRoll is now drawn LAST unconditionally after the optional tumble triple (draw-order contract comment updated), promoting non-landmark slots under RARE_CHANCE with FLAG_RARE, RARE_TINT instanceColor override, and simR *= RARE_SCALE_MUL. The alive-rare list is an Int32Array(2*RARE_LIST_CAP) of (storeIdx, slotGen) pairs pushed only after both store.alloc and pool.alloc succeed, compacted in exactly three hooks (_onAbsorb, _despawnIndex, reset), with oldest-drops-on-overflow; forEachAliveRare(cb(idx,x,y,z,r)) self-skips stale entries via slotGen compare. The sub-pixel sweep skips FLAG_RARE only while tierOf >= tier-1; reinject never sets FLAG_RARE; a latched DEV warn fires when aliveCount > 0.9*ALIVE_TOTAL_BUDGET (re-arms below 0.85). ball.js sizes its flat id table to 60 via ARCH_PER_TIER, folds families as (code % ARCH_PER_TIER) & 7 (landmarks land on proxy families 0/1), cross-asserts all 60 codes against objects.js at boot in DEV, and adds setVisible(b) with reset() restoring group.visible=true. Headless verification: same seed twice produced byte-identical rare sets, different seed differed, all alive landmarks sat at placement j===0, zero rare landmarks, rare rate ~0.23% of 2596 alive (matches RARE_CHANCE), absorb-hook compaction and reset clearing verified, and the old-band rare despawn confirmed via slotGen identity (the naive flags check was a slot-recycling artifact).

**主要な意思決定**:

- archRoll keeps its exact draw position by drawing the raw uniform first and scaling by _wTot8/_wTot10 per branch — the mulberry32 sequence consumed per placement is identical regardless of j, so role upgrades and the j===0 landmark rule cannot perturb determinism.
- rareRoll sits inside the deterministic draw block BEFORE the prewarm fog-skip early-return, so fog-skipped placements still consume it — sequence invariance holds across reload contexts.
- Landmarks consume the rareRoll but are excluded from promotion via ai < 8 (slot test, not weight test).
- Rare promotion writes store.flags = 1|FLAG_RARE at spawn and the golden setColor(RARE_TINT) replaces the palette pick while paletteRoll is still consumed.
- _compactRares is generation-based (slotGen mismatch OR no longer alive-rare) rather than index-targeted, so the three hooks stay one-line calls and stale entries can never linger.
- ball.js boot cross-assert imports ARCHETYPE_ID_BY_CODE from world/objects.js (new top-level dependency, DEV-gated usage; no import cycle — objects.js only imports tuning/tiers/pool).
- yOffsets for all 12 landmarks were measured from the actual normalized geometry in node, not estimated.
- Verified headlessly: rare determinism (same seed => identical set), landmark-only-at-j0, no rare landmarks, compaction hooks, sweep exemption, reset.

**逸脱**:

- DEV alive-population warn is latched with a 0.85 re-arm hysteresis to avoid per-frame console spam (design only said 'DEV warn when aliveCount > 0.9 * ALIVE_TOTAL_BUDGET').
- _resolveCatalog landmark fallback weight is 0.3 (design band midpoint) when a catalog entry is missing, vs 1.0 for absorbable slots — keeps partial-catalog boots representative; design did not specify a fallback.
- RARE_SCALE_MUL is applied to the spawned simR (so physics radius, rest height, and instance scale all grow 1.15x together) — design says 'scale *= RARE_SCALE_MUL' without pinning sim vs visual; this keeps collision and render coherent.
- collisionScale for the 6 landmarks the design left unspecified: soda_bottle 0.7, desk_globe 0.9, bookshelf 0.85, grandfather_clock 0.8, giant_tree 0.8, castle 0.9 (the 6 design-specified values used verbatim: utility_pole 0.45, torii 0.8, pylon 0.5, pagoda 0.7, mountain 0.85, skytree 0.4).
- Rare-list overflow implements 'oldest entry stops sparkling' as a copyWithin shift dropping pair 0 — O(RARE_LIST_CAP) on the rare overflow event only.

### ストリーム D: ゲームシステム（dash物理/absorb/input/runStats）

**ファイル**: `src/physics/ballPhysics.js`, `src/physics/absorb.js`, `src/input/input.js`, `src/game/runStats.js`

**アプローチ**:

ballPhysics.js: constructor now takes an optional bus (default null, all emits null-guarded for headless use); BallState gains dashGauge01 (starts 1) and dashTimer. step() order: (1) recharge gauge += dt/DASH_RECHARGE_S clamped, (2) DASH_READY edge via a private _dashReadyAnnounced flag (true at boot/reset so no spurious chime; cleared only on dash trigger) — this also announces absorb.js top-ups on the next step, keeping ballPhysics the single emitter, (3) dash trigger on intent.dash && gauge>=1: dir = horizontal vel dir if speed >= DASH_DIR_SPEED_K*SPEED_K*r else camera forward (-sin camYaw, 0, -cos camYaw, same convention as intent mapping), vel += dir*DASH_IMPULSE_K*r, gauge=0, dashTimer=DASH_DURATION_S, emit EVT.DASH{gauge01:0}, (4) dashing flag captured BEFORE timer decrement so the trigger frame gets full multipliers: accel *= DASH_ACCEL_MUL and cap *= DASH_CAP_MUL stack multiplicatively with boost, (5) timer decremented at end of step. Gauge/timer are dimensionless/seconds so rescale()/rebase() need no changes. absorb.js: _absorbOne adds ball.dashGauge01 += DASH_ABSORB_GAIN clamped to 1, and stamps PAYLOADS.absorb.rare = (store.flags[i] & FLAG_RARE) !== 0 before the emit and before store.free (free zeroes the flags byte). input.js: _intent gains dash:false; Space keydown (e.repeat guarded so OS key-repeat cannot machine-gun dashes, preventDefault stops page scroll) and #dash-button pointerdown (bound handler, removed in dispose; button lookup guarded for no-DOM) set a _dashLatch consumed by exactly one read(); 'M' keyup sets _muteLatch drained by takeMuteToggle(); blur clears both latches. runStats.js: RunStats(bus, scaleMgr, worldSeed) subscribes ABSORB + MOON_CONTACT in its constructor (main controls the binding subscription order by construction position). addSimTime accumulates sim seconds behind a freeze flag and emits EVT.TIME via an integer-tenths boundary counter (at most one emit per call). The ABSORB handler is allocation-free: objScore = max(1, round(SCORE_SIZE_BASE*sizeReal^SCORE_SIZE_POW)), comboMul clamped at COMBO_SCORE_MAX_MUL, + RARE_SCORE_BONUS, emits EVT.SCORE through PAYLOADS.score. MOON_CONTACT (once-latched) freezes the clock, adds MOON_SCORE_BONUS + lerped time bonus, ranks via RANK_*_S thresholds, persists bests to LS_BEST_KEY with per-sub-record atomic replacement (whole BestRecord swapped, never field-mixed) inside try/catch, and emits EVT.GOAL once; static loadBest() validates version + every record field and returns null on any anomaly (including absent localStorage — the bare reference is inside the try). Verified with a throwaway test tree (shimming Stream C's objects.js exports) running 74 assertions through the real fixed-step loop and synthetic bus events with mocked localStorage; tree deleted after passing.

**主要な意思決定**:

- DASH_READY edge tracked by a private _dashReadyAnnounced flag (not a previous-gauge compare) so absorb.js top-ups to exactly 1.0 still chime on the next step while reset/boot (gauge starts full) never chime — single-emitter rule preserved.
- Dash trigger runs BEFORE the accel/friction/cap block and the dashing flag is read before the timer decrement, so the impulse, accel multiplier and raised cap all apply on the trigger frame itself (the 0.8s burst is exactly DASH_DURATION_S of boosted caps).
- Dash cap/accel multipliers stack multiplicatively with boost (cap = SPEED_K*r*boostMul*dashMul); the vel-dir threshold uses the BASE cap (SPEED_K*r) per the spec wording, matching absorb.js's bonk normalization.
- absorb.js imports FLAG_RARE as a named import from ../world/objects.js per the frozen contract — this import FAILS at module link time until Stream C adds the export (boot is already red until C lands per the lead notes; integration order D->C makes this green).
- Space keydown guards e.repeat — without it the consume-on-read latch would re-latch every OS key repeat and fire a dash per repeat once the gauge refills.
- runStats accesses localStorage as a bare reference inside try/catch (loadBest and the save path), so headless/private-mode environments degrade to null/no-persist without feature detection; tests override globalThis.localStorage.
- RunStats._onAbsorb takes absorbed count from AbsorbEvent.count (absorb.js's authoritative counter) instead of self-incrementing, so the two can never drift.
- newRecord comparisons are strict (timeS < best.timeS, score > best.score): a tied run is not a new record; each improved sub-record is replaced wholesale with all four fields from the same run (atomic, no field mixing).
- addSimTime emits at most one EVT.TIME per call even across a multi-tenth jump (carries the latest timeS) — frames add <= 0.05s so this is the normal single-boundary case anyway.

### ストリーム E: オーディオ+UI（bgm/sfx/hud/screens）

**ファイル**: `src/audio/bgm.js`, `src/audio/sfx.js`, `src/ui/hud.js`, `src/ui/screens.js`

**アプローチ**:

bgm.js (new): class Bgm(bus, initialMuted) owning its OWN AudioContext (gesture listeners + GAME_START bootstrap), with the standard two-clock lookahead scheduler — setInterval(25ms) schedules swung-8th steps up to 0.12s ahead of ctx.currentTime; a 128-step (16-bar) counter indexes a 5-shape chord table (AM7|D9|F#m7|E7sus4->E7 via a per-bar BAR_CHORD lookup) and a 64-entry pentatonic MELODY table with -1 rests and 30ms portamento. Four persistent layer GainNodes (L0 bossa bass/sine-drop kick/rim, L1 detuned-saw stabs through a persistent 1200Hz lowpass + highpassed noise hats, L2 PeriodicWave lead through 2400Hz lowpass, L3 bandpass shaker 16ths + sine arp) feed bgmMaster(0.32)->compressor->destination; TIER_UP ramps layer gains over 1.5s in ctx time AND gates note-node creation on the same tier compare (~51 nodes/s worst case, documented against the 60/s bounded-exemption budget; shared noise AudioBuffer + PeriodicWave hoisted to init). visibilitychange suspends/clearIntervals on hidden and on visible re-anchors nextNoteTime = currentTime + LOOKAHEAD_S keeping the step counter (skip, never burst). MOON_CONTACT cancels+linearRamps master to DUCK_GAIN 0.12 by +0.3s then 0 by +1.8s and stops the scheduler; MOON_CALL spawns a 1.4s swelling sine shimmer; GAME_RESET stops+rewinds; setMuted(true) halts the interval entirely (node budget), unmute re-anchors. Zero setTimeout anywhere in the file. sfx.js: constructor gains initialMuted (applied inside the lazy _ensureCtx master-gain path), setMuted now also skips node creation via a _silent() guard in _note/_noiseBurst/every trigger; ONE shared noise buffer hoisted (roll loop + new bursts); new handlers: DASH whoosh (noise through per-trigger bandpass sweeping 300->2400Hz + saw rise 180->420), DASH_READY E5->A5 chime, SCORE rare 5-note E6-pentatonic gliss + 2.5kHz shimmer, MOON_CALL lowpassed swelling A-major pad 1.2s, MOON_CONTACT 8-note grand fanfare (FANFARE_NOTES extended with G6/A6/C7) over a 2.5s AM9 triangle pad (v1 GAME_WIN fanfare handler REMOVED; roll zeroed at contact), GOAL rank thud (70Hz sine + noise tick) scheduled at ctx.currentTime+1.6s to land on the CSS stamp. hud.js: added diffed writers for #timer (m:ss.t from EVT.TIME 0.1s boundaries), #score-value (ja-JP grouped), #dash-gauge-fill (1% steps from GrowEvent.dashGauge01, instant zero on EVT.DASH, restartable .flash on DASH_READY); 8 pooled .score-float spans created once with deterministic offset tables, recycled via remove-class/reflow/add-class; mute button click emits MUTE_REQUEST and MUTE_CHANGED drives the 🔊/🔇 icon; #moon-arrow positioned from MOON_GUIDE x01/y01 with --arrow-rot = atan2(x-0.5, 0.5-y) pointing at the moon, one 「月へ向かえ！」 toast per run; queue-of-1 3s toast shared by moonCall/rare/guide; HUD hides on MOON_CONTACT, full reset (gauge 1.0, timer 0:00.0, score 0, arrow/toast hidden) on GAME_START. screens.js: EVT.GOAL handler copies all 9 fields into a private object and prebuilds the X intent URL immediately (twitter.com/intent/tweet + encodeURIComponent of the frozen template, formatTime mm:ss.t, formatLength size, toLocaleString('ja-JP') score); MOON_CONTACT pops #flash-overlay.flash via the reflow trick; GAME_WIN with a cached goal runs the staged reveal (.staged on #win-overlay, then .result-reveal at 0.0/0.4/0.9/1.6/2.2s via tracked setTimeouts: time row, score row + 0.8s rAF easeOutCubic count-up, size+detail rows, rank .stamp at exactly +1.6s matching the sfx thud, record badge + best/seed lines + buttons), with a v1 instant-fill fallback when no goal is cached (keeps the interim scaleManager-emitted GAME_WIN path green); #post-x-button does w=window.open(url,'_blank'); if (w===null) location.href=url (no noopener); title 自己ベスト line via the static RunStats.loadBest() refreshed at construction and on GAME_RESET; _clearReveal cancels timers/rAF and strips all staged classes on start/reset.

**主要な意思決定**:

- Bgm layer unlock gates BOTH the gain ramp and note-node creation on tierIndex — saves node budget below each unlock tier while the 1.5s ramp keeps the unlock smooth
- Bgm node-budget arithmetic documented in the header: worst case ~51 short-lived nodes/s (all layers) against the 60/s bounded exemption; persistent per-voice filters (stab LP, hat HP, lead LP, shaker BP) and ONE shared noise buffer + PeriodicWave hoisted to context init
- MOON_CONTACT duck implemented as cancelScheduledValues + setValueAtTime(current) + linearRamp to 0.12 at +0.3s + linearRamp to 0 at +1.8s, scheduler stopped immediately — pure ctx-time, no setTimeout anywhere in bgm.js
- setMuted(true) in both Bgm (interval cleared) and Sfx (_silent() guard in every trigger + _note/_noiseBurst) stops node creation entirely, not just gain=0; unmute in Bgm re-anchors nextNoteTime before restarting
- sfx initialMuted is applied inside _ensureCtx (master.gain.value = muted?0:MASTER_GAIN) so the pre-context mute gap called out in the design is closed; constructor stays backward-compatible (new Sfx(bus) == unmuted)
- hud floating '+N' spans: fixed pool of 8 created once with deterministic offset tables (no Math.random, no DOM churn), animation restarted via the remove/.offsetWidth/add reflow idiom (same idiom for #dash-gauge.flash and #flash-overlay.flash)
- Moon arrow rotation = atan2(x01-0.5, 0.5-y01) deg (top=0°, right=90°) on the --arrow-rot CSS var; position clamped to 4-96%/6-92% so the 56px arrow never clips; 「月へ向かえ！」 toast fired once per run on first active guide
- screens.js keeps a v1 instant-fill GAME_WIN fallback when no GOAL was cached — interim builds (scaleManager still emitting GAME_WIN before Stream A lands) stay functional
- Rank stamp cue hard-coded at 1600ms matching sfx's +1.6s ctx-time thud (both derive from the frozen index.html comment)
- X intent endpoint = https://twitter.com/intent/tweet?text= with the exact frozen text template; URL prebuilt at GOAL from the copied payload, never read from the animating DOM
- Title/result best lines prefer bestTime, fall back to bestScore; all RunStats.loadBest() calls wrapped in try/catch on top of loadBest's own internal catch

**逸脱**:

- Bgm constructor is (eventBus = bus singleton, initialMuted = false) rather than strictly positional Bgm(bus, initialMuted) — call-compatible with the frozen interface, just adds defaults mirroring the v1 Sfx idiom
- bgm.js exports NODE_BUDGET_PER_S (documented constant) in addition to the Bgm class — harmless extra export, not in the frozen interface list
- Touch-device key-hints rewrite in screens.js now also mentions the DASH button (one extra span vs v1) — cosmetic, not specified either way
- HUD #timer uses unpadded minutes (m:ss.t, matching index.html's initial '0:00.0') while the result screen and X post use 2-digit mm:ss.t per the template — the design says 'mm:ss.t' for both; chose consistency with the frozen initial DOM text for the HUD

## Stage 3: 統合 + ブラウザ検証

Integrated all five v2 streams into main.js in the design order D->C->E->B->A against the frozen Phase-0 contracts. main.js changes: replaced the V2-WIRE import block with real imports (Finale/MoonView/RunStats/Bgm/Backdrop, PAYLOADS, LS_MUTE_KEY); injected the bus into BallPhysics (new BallPhysics(bus)) so dash/dashReady emit; moved the ABSORB attach-handler ABOVE all cosmetic consumers and constructed in the binding order main attach-handler -> RunStats -> Effects -> Sfx -> Bgm -> Hud -> Screens (subscription order = dispatch order); read LS_MUTE_KEY before audio construction and passed initialMuted to both Sfx and Bgm; built the finale chain (MoonView -> Finale 7-arg ctor + finale.setEffects(effects)) and wired effects.setRareProvider(spawner.forEachAliveRare.bind(spawner)); implemented the setMutedAll single-owner glue (bgm+sfx setMuted, persist, EVT.MUTE_CHANGED) with one boot-time sync call so the HUD icon matches persisted state, MUTE_REQUEST subscription, and input.takeMuteToggle() drained in frame step 1 (also on the title/win idle branch so M works everywhere); enabled the sole GAME_WIN emission at step 4.5 when finale.state==='done' (safe — Stream A removed the ScaleManager latch); enabled backdrop.update at step 6 (and in the idle branch so the silhouette tracks the title/result camera); added backdrop.setProfileImmediate(devTier) to the ?r= dev-start path; resetWorld's finale.reset()/runStats.reset() stubs now hit the real instances. Deleted WIN_RADIUS_M: import+boot-log usage removed from main.js (log now reports goal@MOON_GOAL_RADIUS_M) and the deprecated export deleted from tuning.js. Cosmetic doc fixes flagged by Stream C: geometryFactory.js header 48->60, POOL_BY_CODE comment stride. Browser validation (agent-browser, session-isolated, vite preview :4321) found two real defects, both fixed: (1) screens.js used the legacy https://twitter.com/intent/tweet?text= endpoint — changed to https://x.com/intent/post?text=; (2) the result-screen buttons sat below the fold at 720px viewport height (post-x-button top at 745px, physically unclickable) — fixed with overflow-y:auto on .overlay plus vh-clamped compaction of the win-overlay (title font, row padding/gaps, rank height) so TIME..buttons fit a 720px window (verified: buttons at y=600-662 post-fix, physical restart click works). Full finale validated three times end-to-end at ?r=480: moonCall toast -> descent with screen-edge arrow + 月へ向かえ toast -> contact flash + HUD hide -> merge/ascension (night fade, star field, golden sparkle fountain, rising moon) -> staged result (time slide-in, score count-up, rank S stamp with .stamp bounce, NEW RECORD badge on record runs and correctly hidden on non-record runs, best+seed lines) -> restart -> second run verified clean (ball visible with stuck objects, HUD back, T5 day palette, gauge full, finale re-arms and can re-trigger descent). X intent URL captured via window.open interception without navigating: full template with mm:ss.t time, rank, ja-JP locale score, size, absorbed/rare counts, #FableKatamari hashtag and pages.dev link. BGM gesture bootstrap verified via an AudioContext constructor patch: exactly 2 contexts (sfx+bgm), both 'running' after the START click. Mute: M-key toggles icon 🔊<->🔇 and persists '1'/'0' to fableKatamari.v2.muted both ways. Dash: gauge instant-zero on Space, ~4s refill (+absorb gain), FOV kick + speed lines visible in screenshot. ?r=4 and ?r=120 tier starts render their palettes/backdrop with zero page errors. Debug overlay (backquote): 46/60 draw calls, 381k/600k tris, heap delta +0KB across samples (zero-alloc law holds), pxr 1.0. No console/page errors in any run (production build). npm run build passes; both AudioContexts, all six screenshots saved to docs/screenshots/.

**統合修正**:

- main.js: full v2 wiring — real Finale/MoonView/RunStats/Bgm/Backdrop construction replacing the V2 STUBs, BallPhysics(bus) injection, LS_MUTE_KEY read before audio, setMutedAll mute ownership + boot icon sync, takeMuteToggle in frame step 1 (and idle branch), GAME_WIN sole-emitter block enabled at step 4.5, backdrop.update at step 6 + idle branch, effects.setRareProvider wiring, backdrop.setProfileImmediate in the ?r= dev path
- main.js: ABSORB attach-handler moved above all cosmetic consumers and construction reordered to the binding subscription order (main attach-handler -> RunStats -> Effects/Sfx -> Hud) — the Phase-0 skeleton had the attach-handler AFTER Effects/Sfx/Hud construction, violating the frozen contract
- main.js + tuning.js: WIN_RADIUS_M fully deleted (import, boot log -> MOON_GOAL_RADIUS_M, deprecated export removed)
- src/ui/screens.js: X intent endpoint changed from legacy https://twitter.com/intent/tweet?text= to https://x.com/intent/post?text= (validation gate requirement)
- index.html: result-screen buttons were below the fold at 720px-tall viewports (post-x-button at y=745, unclickable) — .overlay now overflow-y:auto and #win-overlay vh-compacted (title clamp(30px,min(9vw,6.4vh),96px), row padding/gap clamps, rank-wrap 12vh, rank font 10.5vh); buttons now at y=600-662 in a 720px window
- src/render/geometryFactory.js: stale 'all 48' header comments -> 60 (cosmetic, flagged by Stream C)

**残課題（レビューフェーズへ）**:

- Moon face renders quite dark during ascension/afterglow (thin lit crescent + additive glow ring against the night sky) — readable but moodier than the design's 'glowing THE moon'; cosmetic lever is MoonView glow/emissive tuning (Stream A file)
- Rank thresholds untested against real full playthroughs: ?r=480 dev runs finish in ~5 sim seconds with instant S ranks and ~12-15M scores (T5 mountain absorption is explosive) — the binding Phase-3 task of >=3 full real playthroughs (5cm start) to re-tune RANK_*_S/TIME_BONUS_* is still open
- iOS on-device checks still pending per the design risk note: BGM resume inside the actual START tap and the visibilitychange/pagehide suspend-resume path need a real mobile-Safari device
- Headless Chrome ran at ~10fps (software rendering) — gameplay/finale logic fully verified, but the iGPU profile pass with BGM on (node budget, heap-delta on low-end hardware) and the T3->T4 draw-call watch on real GPU hardware remain integration-checklist items for the deploy pass
- Forced-rescale (KeyR) / forced-rebase screenshot diffs during DESCENT were covered by Stream A's headless tests (moonR exact, drift < 0.04 moonR) but not re-run as in-browser pixel diffs in this pass