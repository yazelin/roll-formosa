/**
 * @file catalog.js — Taipei pack catalog stub (P3).
 *
 * Re-exports Tokyo catalog data as a placeholder. P5 replaces these with
 * real Taipei archetype geometry recipes and display names (zh-TW).
 */
export {
  CATALOG,
  EXTRA_CATALOG,
  DISPLAY_NAME_BY_CODE,
  EXTRA_SIZE_CLASS_BY_CODE,
  EXTRA_POOL_CAPS,
} from '../../config/catalog.js';
