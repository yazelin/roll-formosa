/**
 * @file ending.js — Formosa-island finale, now SHARED across all packs.
 *
 * Island geography is identical for every city, and which pin lights is decided
 * by the engine from the active city id (see ../taiwanEnding.js + endingView.js).
 * So this file is just a re-export — do NOT localize per-city ending data here.
 */
export { ending } from '../taiwanEnding.js';
